/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {},
  i18n: {
    locales: ['nl', 'en'],
    defaultLocale: 'nl',
    localeDetection: false
  }
};
export default nextConfig;
